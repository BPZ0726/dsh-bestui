# Changelog

## 0.1.0 - 2026-08-17

- 首个公开测试预览版，npm 包名为 `dsh-bestui`。
- 支持壁纸、图片取色、浅色/深色双配色、分层透明度与模糊、字体、圆角和界面缩放。
- 支持完整方案导入/导出，并提供静态安装包与动态 Cordis 预览包。
- 发布目标为 DeepSeek Harness Web UI Developer Preview；后续宿主兼容性调整遵循语义化版本。

## Internal build v26

- **中文文案润色**：8 处描述/提示语改为更简洁自然的口吻——简介、「上传后按图片明暗自动切换
  主题」、「导出为 JSON 文件（壁纸、配色、字体、圆角、不透明度等全部设置）；导入后整体替换
  当前方案」、不透明度区说明（「勾选「默认」即完全不透明」）、字体区说明（「字体大小」只缩放
  文字）、界面设置区说明、元素颜色区说明、「恢复界面默认」按钮等。
- **设置面板视觉优化**：区块间距与内边距更舒展（`padding/gap` 微调）；标题层级加粗
  （`font-weight 600/500`）；分区标题悬停高亮；颜色行悬停背景；按钮/下拉框过渡动画；
  下拉框指针光标与边框过渡；数值列加宽（`min-width 44px`）；分组标签加粗；新增键盘
  焦点环（`:focus-visible`）。
- 新增 `scripts/polish-zh.mjs`：以英文锚点定位 `t(zh, en)` 调用、仅替换中文段的文案
  维护工具（其它字节不动）。

## v25

- 品牌名从「Best UI」正式改为 **BestUI**（设置行标题、插件注册名、README、仓库目录）。

## v24

- **库存色板对齐**：14 个主题色默认值改为产品 `design-platform.css` 的原始默认配色，
  「恢复默认配色」即还原产品原样；旧方案自动迁移到新色板（`LEGACY_NEUTRAL_PAIRS`）。
- **元素名校对**：第三层表面（含菜单）、浮层/弹窗、选中与按下背景、反色文字与图标、
  三级文字、强边框、滚动条滑块（悬停）、提示气泡背景——均与宿主实际消费场景对应。
- **死变量清理**：移除产品未使用的 `toast-bg`、`bg-skeleton`、`interactive-bg-hover-accent`；
  恢复被误删且确实被气泡组件消费的 `tooltip-bg`。
- 取色器支持 3/4/8 位十六进制简写（`cssToPicker`）。
- 关闭「启用」时自定义配色仍然生效（颜色独立于壁纸开关）。
- 文字色相为 0 时颜色原样透传，避免 hex→hsl→hex 取整漂移。

## v23

- 修复浅色额外颜色变量失效 bug：从 `:root` 改为直接声明在 `body` 并加 `!important`，
  避免被宿主 body 级同名声明覆盖。
- 新增「导出方案 / 导入方案」：导出完整 UI 方案为 JSON（含壁纸、配色、字体、圆角、
  不透明度），导入时整体替换并完整校验与旧数据迁移。

## v22

- 删除无效的「第一层表面 / 第三层菜单 / 浮层弹窗」三个不透明度调整——这些表面在产品
  界面中不可见透出，改为固定不透明（颜色自定义保留）。
- 「不透明度」区现提供背景基底、第二层表面、侧边栏三项；旧设置自动迁移。

## v21

- 首个可用版本：壁纸上传、逐表面不透明度、按浅/深方案自定义元素颜色、自动配色、
  字体与界面设置、主题 token 覆盖层架构。
